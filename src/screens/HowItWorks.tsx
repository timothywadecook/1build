import React from "react";

interface Props {}

const HowItWorks: React.FC<Props> = (props) => (
  <div className="screen">
    <h2 style={{ paddingLeft: 10 }}>How It Works</h2>
  </div>
);

export default HowItWorks;
