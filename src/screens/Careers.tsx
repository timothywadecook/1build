import React from "react";

interface Props {}

const Careers: React.FC<Props> = (props) => (
  <div className="screen">
    <h2 style={{ paddingLeft: 10 }}>Careers</h2>
  </div>
);

export default Careers;
