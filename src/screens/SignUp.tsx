import React from "react";

interface Props {}

const SignUp: React.FC<Props> = (props) => (
  <div className="screen">
    <h2 style={{ paddingLeft: 10 }}>Sign Up</h2>
  </div>
);

export default SignUp;
