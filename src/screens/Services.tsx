import React from "react";

interface Props {}

const Services: React.FC<Props> = (props) => (
  <div className="screen">
    <h2 style={{ paddingLeft: 10 }}>Our Services</h2>
  </div>
);

export default Services;
