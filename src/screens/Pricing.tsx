import React from "react";

interface Props {}

const Pricing: React.FC<Props> = (props) => (
  <div className="screen">
    <h2 style={{ paddingLeft: 10 }}>Simple Pricing</h2>
  </div>
);

export default Pricing;
